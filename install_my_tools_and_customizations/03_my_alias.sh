#!/bin/bash

# ======================================================================================================================
# ALiases: shortcuts to be stored and used by me to ease my work:
# ======================================================================================================================
alias cls='clear'  # Clear the terminal screen
alias ll='ls -la'  # List all files with detailed information
alias ..='cd ..'   # Move up one directory
alias ...='cd ../..' # Move up two directories

# alias gs='git status' # Check the status of a Git repository
# alias ga='git add .' # Stage all changes for commit
# alias gc='git commit -m' # Commit staged changes with a message
# alias gp='git push' # Push commits to the remote repository
# alias gl='git log --oneline --graph --decorate' # View a concise Git log

# alias h='history' # Show command history
# alias j='jobs -l' # List all current jobs with their process IDs
# alias psg='ps aux | grep' # Search for processes by name

# alias df='df -h' # Display disk space usage in human-readable format
# alias du='du -h --max-depth=1' # Show disk usage of directories with a depth of 1

# alias free='free -h' # Display memory usage in human-readable format
# alias mkdir='mkdir -p' # Create directories, including parent directories as needed

# alias wget='wget -c' # Continue downloading partially downloaded files
# alias aosp13='cd ~/aosp13' # Navigate to the AOSP 13 directory
# alias aosp14='cd ~/aosp14' # Navigate to the AOSP 14 directory
# alias aosp15='cd ~/aosp15' # Navigate to the AOSP 15 directory
# alias rpi5='cd ~/aosp_rpi5' # Navigate to the AOSP RPi5 directory
# alias llr='ls -laR' # List all files recursively with detailed information
# alias path='echo -e ${PATH//:/\\n}' # Display each directory in PATH on a new line
# alias ip='ip addr show' # Show